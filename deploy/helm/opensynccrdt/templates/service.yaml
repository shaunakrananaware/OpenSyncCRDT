apiVersion: v1
kind: Service
metadata:
  name: {{ include "opensynccrdt.fullname" . }}
  labels:
    {{- include "opensynccrdt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "opensynccrdt.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
