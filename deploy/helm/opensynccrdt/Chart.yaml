apiVersion: v2
name: opensynccrdt
description: OpenSyncCRDT — a local-first sync engine (single binary) with CRDT sync over WebSocket.
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Application version this chart deploys by default.
appVersion: "0.1.0"
home: https://github.com/opensynccrdt/opensynccrdt
sources:
  - https://github.com/opensynccrdt/opensynccrdt
keywords:
  - crdt
  - sync
  - local-first
  - websocket
  - automerge
