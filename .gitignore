# Legacy prototype (Hocuspocus-based), kept out of the Go module.
crdt-app-client
crdt-app-server
.claude
docker-compose.yml

# Build artifacts
/opensynccrdt
/dist/

# Local data directory + SQLite databases (WAL/SHM sidecar files included)
/data/
*.db
*.db-wal
*.db-shm

# Local config overrides
opensynccrdt.yaml
config.yaml
config.yml
